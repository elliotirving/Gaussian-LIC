# Read automatically by `docker compose -f docker/docker-compose.yml ...`.
# Host directory mounted read-write at /data inside the container. Point it at
# wherever your bags/mcaps live; the converters write their output back here too.
DATA_DIR=/home/rsl/data
